const TRPC='https://proxy.european-athletics.com/trpc';
const COMPETITION_CODE='ECH26';
const IDS=['ATHWHEPTATH-----------JT----------','ATHWHEPTATH-----------JT--A00100--','ATHWHEPTATH-----------JT--B00100--'];
function enc(payload){return encodeURIComponent(JSON.stringify({json:payload}));}
async function call(proc,payload){const url=`${TRPC}/${proc}?input=${enc(payload)}`;try{const r=await fetch(url,{headers:{'X-Client-Platform':'Desktop','Accept':'application/json','User-Agent':'Mozilla/5.0'},cf:{cacheTtl:0,cacheEverything:false}});const text=await r.text();let body;try{body=JSON.parse(text);}catch{body=text.slice(0,30000);}return{proc,payload,status:r.status,body};}catch(e){return{proc,payload,error:String(e)}}}
export async function onRequestGet(){const out=[];out.push(await call('liveResults.getAthletesFeed',{competitionCode:COMPETITION_CODE}));for(const id of IDS){out.push(await call('liveResults.getCombinedEventResultsFeed',{event:id,competitionCode:COMPETITION_CODE,isSummary:true}));out.push(await call('liveResults.getCombinedEventResultsFeed',{event:id,competitionCode:COMPETITION_CODE,isSummary:false}));}return new Response(JSON.stringify(out,null,2),{headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store'}});}
