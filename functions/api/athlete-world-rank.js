const SOURCE='https://worldathletics.nimarion.de';
function response(data,status=200){return new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'public, max-age=300'}});}
function combinedRank(profile,type){const rows=Array.isArray(profile?.currentWorldRankings)?profile.currentWorldRankings:[];const rx=type==='women'?/heptathlon|combined/i:/decathlon|combined/i;const hit=rows.find(r=>rx.test(String(r?.eventGroup||'')))||null;return hit&&Number.isFinite(Number(hit.place))?Number(hit.place):null;}
export async function onRequestGet({request}){const u=new URL(request.url),id=String(u.searchParams.get('id')||'').trim(),type=u.searchParams.get('type')==='women'?'women':'men';if(!id)return response({worldRank:null,error:'Mangler utøver-ID'},400);try{const r=await fetch(`${SOURCE}/athletes/${encodeURIComponent(id)}`,{headers:{Accept:'application/json','User-Agent':'Mangekampanalyse/2.0'},cf:{cacheTtl:300,cacheEverything:true}});if(!r.ok)return response({worldRank:null});const profile=await r.json();return response({worldRank:combinedRank(profile,type),rankings:profile?.currentWorldRankings||[]});}catch(e){return response({worldRank:null,error:String(e?.message||e)});}}
